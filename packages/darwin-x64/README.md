# @trayjs/darwin-x64

macOS x64 native binary for [@trayjs/trayjs](https://www.npmjs.com/package/@trayjs/trayjs).

This package is installed automatically as a platform-specific dependency. There is no need to install it directly.
